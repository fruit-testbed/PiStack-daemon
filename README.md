# Daemon for running on the Pi
THIS IS IN PROGRESS.  The code was written to test the functionality without any thought for 'proper' implementation.

## Installation

```sudo apt install python3-configargparse python3-rpi.gpio```
